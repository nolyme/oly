{
  "private": true,
  "devDependencies": {
    "@types/jest": "19.2.3",
    "@types/node": "7.0.18",
    "jest": "20.0.3",
    "lerna": "2.0.0-rc.4",
    "rimraf": "2.6.1",
    "ts-jest": "20.0.3",
    "tslint": "5.2.0",
    "tslint-react": "3.0.0",
    "typescript": "2.3.2"
  },
  "scripts": {
    "docs": "node packages/oly-docs/bin/oly-docs",
    "docs-json": "JSON=true DIRECTORY_OUT=packages/oly-docs/test node packages/oly-docs/bin/oly-docs",
    "all": "lerna run clean; lerna bootstrap; lerna run build; lerna run test",
    "clean": "lerna run clean",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:ci": "jest --coverage",
    "publish": "lerna publish --force-publish=*"
  },
  "jest": {
    "preset": "./jest-preset.json",
    "transform": {
      ".(ts|tsx)": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    },
    "collectCoverageFrom": [
      "packages/**/src/**/*.{ts,tsx}",
      "!packages/oly/**/*.{ts,tsx}",
      "!packages/oly-test/src/**/*.{ts,tsx}",
      "!packages/oly-docs/src/**/*.{ts,tsx}",
      "!packages/oly-tools/**/*.{ts,tsx}"
    ],
    "globals": {
      "__TS_CONFIG__": "packages/oly-core/test/tsconfig.json"
    }
  }
}
